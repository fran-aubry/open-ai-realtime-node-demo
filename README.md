# open-ai-realtime-node-demo
Demo on how to use the OpenAI real-time API with Node.js

# Voice Example

To run the voice examples, we need to install SoX.

## macOC

```
brew install sox
```

## Linux (Debian/Ubuntu)

```
sudo apt update
sudo apt install sox
```